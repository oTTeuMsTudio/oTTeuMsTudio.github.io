name: Deploy site

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch packaged game builds
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p firstgame/packaged/android firstgame/packaged/windows
          gh release download fgiu --repo "$GITHUB_REPOSITORY" --pattern 'FGIU-arm64.apk' --dir firstgame/packaged/android
          gh release download fgiu --repo "$GITHUB_REPOSITORY" --pattern 'FGIU-Windows.zip' --dir firstgame/packaged/windows
          ls -lh firstgame/packaged/android firstgame/packaged/windows

      - name: Stage site
        run: |
          mkdir _site
          rsync -a --exclude '.git' --exclude '_site' --exclude '.github' ./ _site/
          touch _site/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
